apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: cert-manager
spec:
  repo: https://charts.jetstack.io
  chart: cert-manager
  version: v1.14.2
  valuesContent: |-
    installCRDs: "true"
