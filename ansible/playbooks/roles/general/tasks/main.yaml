---
- name: "Ensure common CLI tools are updated with brew."
  community.general.homebrew:
    name: "{{ item }}"
    state: latest
  loop:
    - ripgrep
    - fd
    - jq
    - yq
    - fzf
    - tree
    - shellcheck
    - shfmt
    - bat
    - eza
    - gh
