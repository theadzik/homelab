apiVersion: policies.kyverno.io/v1
kind: ImageValidatingPolicy
metadata:
  name: validate-image-attestations
spec:
  webhookConfiguration:
    timeoutSeconds: 30
  failurePolicy: Fail
  # Only validate Pods. Without this, Kyverno autogenerates rules for pod
  # controllers (Deployments, Jobs, CronJobs, ...), re-verifying the same
  # image on every controller admission on top of the Pod itself.
  autogen:
    podControllers:
      controllers:
        - none
  evaluation:
    background:
      enabled: true
  validationActions:
    - Deny
  matchConditions:
    - name: only-theadzik-images
      expression: >-
        object.spec.containers.exists(c, c.image.startsWith("ghcr.io/theadzik/"))
        || (has(object.spec.initContainers)
        && object.spec.initContainers.exists(c, c.image.startsWith("ghcr.io/theadzik/")))
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE"]
        resources: ["pods"]
  matchImageReferences:
    - glob: "ghcr.io/theadzik/*"
  attestors:
    - name: cosign
      cosign:
        keyless:
          identities:
            - issuer: "https://token.actions.githubusercontent.com"
              subjectRegExp: "^https://github\\.com/theadzik/github-workflows/\\.github/workflows/build-and-push\\.yaml@.+$"
        ctlog:
          url: https://rekor.sigstore.dev
  validationConfigurations:
    mutateDigest: true
    required: true
    verifyDigest: true
  validations:
    - expression: >-
        (images.containers + images.initContainers + images.ephemeralContainers).map(image,
          verifyImageSignatures(image, [attestors.cosign])
        ).all(e, e > 0)
      message: Failed to verify image signature
