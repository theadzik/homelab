provider:
  name: pihole
env:
  - name: EXTERNAL_DNS_PIHOLE_PASSWORD
    valueFrom:
      secretKeyRef:
        name: web-password
        key: WEBPASSWORD
extraArgs:
  pihole-server: "https://pihole.zmuda.pro:8443"
  pihole-api-version: "6"
labelFilter: dns-type in (internal)
registry: noop
policy: sync
