features:
  backgroundScan:
    # Each scan re-verifies every matched pod against Rekor and the registry.
    backgroundScanInterval: 6h

reportsController:
  resources:
    limits:
      memory: 256Mi
